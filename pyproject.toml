[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "leasehold"
version = "0.1.0"
description = "In-process lease table for key ownership with TTL (stdlib only)"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["leasehold"]
